apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: 
  namespace: %namespace%
spec:
  ingressClassName: 
  rules:
  - http:
      paths:
      - path:
        pathType: Prefix
        backend:
          service:
            name: 
            port:
              number: 80

